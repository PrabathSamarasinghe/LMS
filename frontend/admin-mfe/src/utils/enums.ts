enum Severity {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}

enum Type {
  TEXT = "text",
  EMAIL = "email",
  TEL = "tel",
  DATE = "date",
  SELECT = "select",
}

enum Gender {
  Male = "Male",
  Female = "Female",
  Other = "Other",
}

enum Status {
  Active = "Active",
  Inactive = "Inactive",
}

export { 
    Severity,
    Type,
    Gender,
    Status,
}